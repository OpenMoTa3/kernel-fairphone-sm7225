name: Build Droidian Kernel Package

on:
  workflow_dispatch:
  push:
    branches:
      - droidian

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL

      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup CCache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.ccache
          key: fp4-kernel-ccache-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            fp4-kernel-ccache-${{ runner.os }}-

      - name: Apply droidian.config to defconfig
        run: |
          # 将 droidian 专有配置合并到基准 defconfig
          if [ -f "droidian/fp4.config" ]; then
            cat droidian/fp4.config >> arch/arm64/configs/ubports_fp4_defconfig
          fi

      - name: Build Kernel Package via Droidian Container
        run: |
          mkdir -p "${{ github.workspace }}/pkgs"
          mkdir -p "${{ github.workspace }}/.ccache"

          docker run --rm \
            -v "${{ github.workspace }}/pkgs:/buildd" \
            -v "${{ github.workspace }}:/buildd/sources" \
            -v "${{ github.workspace }}/.ccache:/root/.ccache" \
            -e CCACHE_DIR=/root/.ccache \
            -e CCACHE_MAXSIZE=5G \
            -w /buildd/sources \
            quay.io/droidian/build-essential:current-amd64 \
            bash -c "
              set -e
              git config --global --add safe.directory /buildd/sources
              
              dpkg --add-architecture arm64
              apt-get update
              apt-get install -y linux-packaging-snippets \
                binutils-aarch64-linux-gnu \
                binutils-arm-linux-gnueabi \
                device-tree-compiler \
                ccache \
                linux-initramfs-halium-generic:arm64
              
              # 自动生成控制文件
              rm -f debian/control debian/compat
              debian/rules debian/control
              sed -i 's/clang-android-, //g' debian/control
              
              # 调用官方全自动构建命令
              RELENG_HOST_ARCH=arm64 releng-build-package
            "

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: droidian-fp4-kernel
          path: ${{ github.workspace }}/pkgs/*
